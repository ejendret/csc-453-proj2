#include "threadlist.h"

ThreadList * initThreadList() {
    ThreadList * list = (ThreadList*)malloc(sizeof(CircularLinkedList));
}

thread addThread(ThreadList * list, thread new_thread){
    threadToAdd = NULL;

    // If data is null, don't add
    if (data == NULL)
    {
        return threadToAdd;
    }

    if (list->head == NULL)
    {
        list->head = 
    }
}
